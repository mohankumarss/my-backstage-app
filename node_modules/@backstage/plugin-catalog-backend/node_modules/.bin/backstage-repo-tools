#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../repo-tools/bin/backstage-repo-tools" "$@"
  ret=$?
else 
  node  "$basedir/../../../repo-tools/bin/backstage-repo-tools" "$@"
  ret=$?
fi
exit $ret
